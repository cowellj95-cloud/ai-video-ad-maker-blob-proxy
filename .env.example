# Vercel Blob bypass token for image access authorization
# Required for the proxy to serve private images
VERCEL_BYPASS_TOKEN=your_bypass_token_here

# Vercel Blob read/write token (only needed locally for testing)
BLOB_READ_WRITE_TOKEN=your_blob_token_here
